# Managed by Ansible
[Unit]
Description=Run due WordPress cron events
Wants=network-online.target
After=network-online.target docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker compose -f /opt/stacks/web/compose.yml run --rm -T wpcli wp cron event run --due-now
