---
- name: Deploy nginx vhosts
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: /opt/web/nginx/conf.d/{{ item }}
    mode: "0644"
  loop:
    - site.conf
    - blog.conf
    - status.conf
  notify: Reload nginx
